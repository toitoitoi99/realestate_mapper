{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) out=$(/usr/bin/python3 -m py_compile \"$f\" 2>&1) || echo \"[hook] py_compile failed: $out\" ;; esac"
          }
        ]
      }
    ]
  }
}
